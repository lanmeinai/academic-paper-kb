# Skill：导出参考文献

## 触发方式
用户说「导出参考文献」或「导出 BibTeX」时执行本 Skill。

## 执行流程

### 第一步：获取筛选条件
询问用户：
- 按主题关键词筛选？还是手动指定论文标题列表？
- 需要多少篇？（默认全部匹配）
- 输出格式：BibTeX / APA / 编号列表？

### 第二步：从 data/index.json 筛选论文

### 第三步：生成输出文件
保存到项目根目录 exports/<日期>_<主题>.bib（或 .txt）

### 第四步：同时输出简要列表
在终端打印：序号、中文标题、作者、年份，方便核对。
